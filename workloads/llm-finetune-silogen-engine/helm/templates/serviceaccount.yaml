{{- if or (and .Values.aimManifest.modelManifest.enabled (not .Values.aimManifest.serviceAccountName) .Values.aimManifest.modelManifest.autoApply) (and .Values.aimManifest.adapterManifest.enabled (not .Values.aimManifest.serviceAccountName) .Values.aimManifest.adapterManifest.autoApply) }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{{ .Release.Name }}-sa"
  {{- if .Values.labels }}
  labels:
    {{- range $label, $value := .Values.labels }}
    {{ $label }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
{{- end }}
